server:
  port: ${APP_PORT:8080}

spring:
  # 从额外的本地文件导入开发环境的敏感配置
  # env-config.yml 会被 .gitignore 忽略，不会提交到 Git
  # 本地开发时使用文件配置，Docker 部署时使用环境变量
  config:
    import:
      - optional:classpath:env-config.yml


